rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read/write access for now (change this for production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
